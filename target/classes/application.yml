spring:
  application:
    name: learning-rest-with-spring-boot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/learningdb?useTimezone=true&serverTimezone=UTC
    username: root
    password: adm123
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
logging:
  level:
    com.lucasbrasileiro: DEBUG

spring-doc:
  paths-to-match: /api/**/v1/**
  swagger-ui:
    user-root-path: true